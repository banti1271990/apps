union:

the number of columns must be same in the queries of union:

select ename from emp union select name from stdel;

union does not return duplicates
----------------------------------------------
union all:

the number of columns must be same in the queries of union all:

select ename from emp union all select name from stdel;

union all returns duplicates
